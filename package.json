{
  "name": "e-pelara-monorepo",
  "private": true,
  "scripts": {
    "lint": "npm --prefix frontend run lint && npm --prefix frontend/rkpd-admin run lint && npm --prefix backend run lint",
    "format": "npm --prefix frontend run format && npm --prefix frontend/rkpd-admin run format && npm --prefix backend run format",
    "guard:final-lock": "node scripts/check-final-locked-files.js",
    "guard:hash-lock-key": "node scripts/hash-final-lock-key.js",
    "prepare": "husky",
    "check:workflow-compliance-staged": "node scripts/checkWorkflowComplianceStaged.js"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.2.10"
  },
  "lint-staged": {
    "backend/models/*.js": "npm run check:workflow-compliance-staged --"
  }
}
