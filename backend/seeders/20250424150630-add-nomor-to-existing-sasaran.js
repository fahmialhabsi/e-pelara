module.exports = {
  up: async (queryInterface, Sequelize) => {
    // Data seeder bisa ditambahkan nanti jika diperlukan.
  },

  down: async (queryInterface, Sequelize) => {
    // Hapus data seeder jika ada.
  },
};
